# PodDisruptionBudgets ensure at least one replica survives voluntary disruptions
# (node drains, rolling upgrades) for the stateful critical components.
{{- if or (gt (int (.Values.apiGateway.replicaCount | default 1)) 1) (.Values.autoscaling.enabled) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ai-agent-sandbox.fullname" . }}-api-gateway-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api-gateway
---
{{- end }}
{{- if or (gt (int (.Values.litellm.replicaCount | default 1)) 1) (.Values.autoscaling.enabled) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ai-agent-sandbox.fullname" . }}-litellm-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: litellm
---
{{- end }}
{{- if gt (int (.Values.operator.replicaCount | default 2)) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ai-agent-sandbox.fullname" . }}-operator-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: operator
{{- end }}
