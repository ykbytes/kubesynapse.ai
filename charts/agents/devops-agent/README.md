# devops-agent

Installs a DevOps-focused `AIAgent` tuned for CI/CD, delivery risk review,
deployment planning, and operational readiness checks.

```bash
helm install devops-agent ./charts/agents/devops-agent -n default
```
