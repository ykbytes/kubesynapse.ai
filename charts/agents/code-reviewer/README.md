# code-reviewer

Installs a review-oriented `AIAgent` tuned for patch review, regression
spotting, and concise actionable feedback.

```bash
helm install code-reviewer ./charts/agents/code-reviewer -n default
```
