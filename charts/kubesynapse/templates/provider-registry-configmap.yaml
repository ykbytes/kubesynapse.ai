apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kubesynapse.fullname" . }}-provider-registry
  labels:
    {{- include "kubesynapse.labels" . | nindent 4 }}
data:
  providers.json: |
    {"version":1,"custom_providers":{}}
